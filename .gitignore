# Secrets & environment
.env
.env.*
!.env.example

# Python
.venv/
venv/
__pycache__/
*.py[cod]
.mypy_cache/
.ruff_cache/
.pytest_cache/
.coverage
*.egg-info/
build/
dist/

# Node
node_modules/

# Data & model artifacts
data/
*.parquet
models/
*.ckpt

# Power BI / Fabric local artifacts
*.pbix
*.abf
.pbi/

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# Private build journal — its own repo (fabric-plan-fpna-journal).
# Step-by-step phase guides, learning log, plan and agent instructions: they
# document how this was built, not what it is. Public equivalent: docs/build-log.md.
docs/private/

# Agent instructions. Must stay at the repo root to be loaded, so it is ignored
# rather than moved; the version-controlled copy lives in docs/private/CLAUDE.md.
/CLAUDE.md

# Screen recordings — demo footage is hosted as a GitHub release asset, never committed
*.mp4
*.mkv
*.webm
